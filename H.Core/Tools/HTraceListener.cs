using System;
using System.Diagnostics;
using System.IO;

namespace H.Core.Tools
{
    public class HTraceListener
    {
        public static void AddTraceListener()
        {
            //var LogFileName = "HolosOutput.txt";
            //string LogFilePath = Environment.ExpandEnvironmentVariables("%LOCALAPPDATA%" + $"\\HOLOS_4\\{LogFileName}");
            //FileStream filestream = new FileStream(LogFilePath, FileMode.OpenOrCreate, FileAccess.Write, FileShare.Write);
            //TextWriterTraceListener listener = new TextWriterTraceListener(filestream);
            //Trace.Listeners.Add(listener);
        }

    }
}
